<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_move_form" model="ir.ui.view">
        <field name="name">account.move</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='other_info']" position="before">
                <page string="Transportista">
                    <group>
                        <group string="Transportista">
                            <field name="transfer_type_id"/>
                            <field name="transfer_reason_id"/>
                            <field name="transfer_start_date"/>
                            <field name="scheduled_transfer"/>
                            <field name="driver_id"/>
                            <field name="transport_means"/>
                            <field name="docker_number"/>
                            <field name="code_port"/>
                        </group>
                    </group>
                </page>
            </xpath>
        </field>
    </record>
    <record id="view_invoice_tree" model="ir.ui.view">
        <field name="name">account.invoice..tree</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_invoice_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//tree" position="attributes">
                <attribute name="multi_edit">1</attribute>
            </xpath>
            <xpath expr="//field[@name='invoice_date_due']" position="after">
                <field name='transport_means'></field>
            </xpath>
        </field>
    </record>
</odoo>